<footer>
  <p>&#169;{new Date().getFullYear()}</p>
</footer>
<style>
  footer {
    width: 100%;
    height: 120px;
    color: var(--white-text);
    text-align: center;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding: var(--gap-size);
    background-color: var(--dark);
    padding: var(--gap-size) 0 calc(var(--gap-size) * 3) 0;
  }
</style>
